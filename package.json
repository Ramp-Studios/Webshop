{
  "name": "webshop-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon src/server.js",
    "browser-windows": "start /max chrome.exe http://localhost:5000",
    "development-linux": "sudo docker-compose up --build -d && xdg-open http://localhost:5000",
    "development-windows": "docker-compose up --build -d && start /max chrome.exe http://localhost:5000",
    "debug-windows": "docker-compose up --build && start /max chrome.exe http://localhost:5000",
    "debug-linux": "sudo docker-compose up --build && xdg-open http://localhost:5000"
  },
  "author": "R C Springer, P Thong",
  "license": "ISC",
  "homepage": "",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "config": "^3.2.4",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-validator": "^6.3.1",
    "gravatar": "^1.8.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.1",
    "multer": "^1.4.2",
    "nodemon": "^2.0.2",
    "request": "^2.88.0"
  }
}
